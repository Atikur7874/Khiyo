services:
  mongo:
    image: mongo:7
    container_name: terrarium-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - terrarium_mongo_data:/data/db

volumes:
  terrarium_mongo_data:
